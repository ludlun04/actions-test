meta {
  name: put-cat
  type: http
  seq: 2
}

put {
  url: http:localhost:8080/api
  body: text
  auth: inherit
}

body:text {
  Hello Kitty
}

assert {
  res.status: eq 201
  res.body: eq 3
}

settings {
  encodeUrl: true
  timeout: 0
}
